extends parentLayout

block content
    if !areAnySavedRatings
        p No records saved for this student yet. Come back later
    else
        each record in ratings
            div(class="studentNotes")
                p #{record.ratingDate}
                p #{record.numberRating}
                p #{record.note}